<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neal.myblog.mapper.UserMapper">
    <resultMap id="user" type="com.neal.myblog.entity.TUser">
        <id column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="user_pass" jdbcType="VARCHAR" property="userPass"/>
    </resultMap>
    <select id="login" resultMap="user" parameterType="com.neal.myblog.entity.TUser">
        SELECT *
        FROM t_user
        WHERE user_name = #{userName} AND user_pass = #{userPass}
    </select>
</mapper>